#include<bits/stdc++.h>
using namespace std;

class Node
{

public :

    int value;
    Node *next;

    Node(int value)
    {
        this->value = value;
        this->next = NULL;
    }
};

inset_last(Node* &head,Node* &tail,int val)
{

    Node *head = new Node(val);

    if(head == NULL)
    {

        head = newNode;
        tail = newNode;
    }

}
